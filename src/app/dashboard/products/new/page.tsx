import Link from "next/link";
import { createProductAction } from "@/app/actions/product";

export default async function NewProductPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) {
  const { error } = await searchParams;
  return <main className="app-main"><div className="shell" style={{maxWidth:760}}><div className="page-head"><div><h1 className="display">Produk baru</h1><p>Buat kursus digital atau layanan jasa yang dikerjakan oleh tim.</p></div><Link className="btn" href="/dashboard">Batal</Link></div><section className="panel form-panel">{error && <p className="alert">{error}</p>}<form className="form" action={createProductAction}><div className="field"><label htmlFor="type">Jenis produk</label><select className="input" id="type" name="type" defaultValue="SERVICE"><option value="SERVICE">Jasa / layanan</option><option value="COURSE">Kursus digital</option></select><small className="field-hint">Jasa memiliki formulir klien, dokumen, dan status pengerjaan.</small></div><div className="field"><label htmlFor="name">Nama produk</label><input className="input" id="name" name="name" required minLength={3} placeholder="Contoh: Pendirian PT Perorangan" /></div><div className="field"><label htmlFor="headline">Headline</label><input className="input" id="headline" name="headline" required minLength={10} placeholder="Hasil utama yang didapat pembeli" /></div><div className="field"><label htmlFor="description">Deskripsi</label><textarea className="input" id="description" name="description" required minLength={20} placeholder="Jelaskan cakupan, proses, dan hasil layanan." /></div><div className="field"><label htmlFor="price">Harga (Rupiah)</label><input className="input" id="price" name="price" type="number" min={10000} step={1000} required defaultValue={99000} /></div><button className="btn btn-primary" type="submit">Simpan produk</button></form></section></div></main>;
}
